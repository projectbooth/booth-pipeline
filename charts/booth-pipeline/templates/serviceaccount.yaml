{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "booth-pipeline.serviceAccountName" . }}
  labels:
    {{- include "booth-pipeline.labels" . | nindent 4 }}
{{- end }}
